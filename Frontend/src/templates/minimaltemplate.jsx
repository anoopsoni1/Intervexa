import StyleTemplateHub from "./StyleTemplateHub.jsx";

/** Minimal resume / portfolio hub — for all users. */
export default function MinimalTemplate() {
  return <StyleTemplateHub styleId="minimal" />;
}
