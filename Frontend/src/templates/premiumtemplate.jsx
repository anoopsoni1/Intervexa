import StyleTemplateHub from "./StyleTemplateHub.jsx";

/** Premium resume / portfolio hub — for all users. */
export default function PremiumTemplate() {
  return <StyleTemplateHub styleId="premium" />;
}
