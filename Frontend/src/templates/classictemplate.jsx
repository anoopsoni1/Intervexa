import StyleTemplateHub from "./StyleTemplateHub.jsx";

/** Classic resume / portfolio hub — for all users. */
export default function ClassicTemplate() {
  return <StyleTemplateHub styleId="classic" />;
}
