import ResumeDesignListPage from "./ResumeDesignListPage.jsx";

/** Minimal resume design list — for all users. */
export default function MinimalResumeDesignViewPage() {
  return (
    <ResumeDesignListPage styleKey="minimal" hubPath="/templates/minimal" styleTitle="Minimal" />
  );
}
