# Production build: by default the app calls VITE_API_BASE_URL or https://intervexa.onrender.com (see src/config).
# Only set VITE_USE_SAME_ORIGIN_API=true if your host proxies /api/* to the backend (vercel.json or public/_redirects).
# VITE_USE_SAME_ORIGIN_API=true
